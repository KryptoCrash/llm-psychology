[project]
name = "llm-psychology"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "transformer-lens>=3.0.0",
    "transformers>=5.5.4",
]
